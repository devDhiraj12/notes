apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config-map
data:
  mysql-server: mysql    # The name of mysql service
  mysql-database-name: userdata     # The name of the database used by the demo app
  mysql-user-username: root1
